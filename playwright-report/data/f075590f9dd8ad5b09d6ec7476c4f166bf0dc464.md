# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth-navigation.spec.ts >> Authentication and navigation >> redirects unauthenticated users away from protected users page
- Location: tests/e2e/auth-navigation.spec.ts:28:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected: "http://127.0.0.1:5173/"
Received: "http://127.0.0.1:5173/users"
Timeout:  5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × unexpected value "http://127.0.0.1:5173/users"

```

```yaml
- text: "{\"rows\":[{\"id\":1,\"user_id\":1,\"first_name\":\"Ada\",\"last_name\":\"Admin\",\"email\":\"ada.admin@example.com\",\"province\":\"Cebu\",\"municipality\":\"Cebu City\",\"user_type\":1},{\"id\":2,\"user_id\":2,\"first_name\":\"Rina\",\"last_name\":\"Reviewer\",\"email\":\"rina.reviewer@example.com\",\"province\":\"Bohol\",\"municipality\":\"Tagbilaran\",\"user_type\":2}],\"count\":2}"
```

# Test source

```ts
  1  | import playwright from '@playwright/test';
  2  | import { setupApiMocks } from './helpers/mockApi';
  3  | 
  4  | const { test, expect } = playwright;
  5  | 
  6  | test.describe('Authentication and navigation', () => {
  7  |   test('opens login drawer and signs in', async ({ page }) => {
  8  |     await setupApiMocks(page, { authenticated: false });
  9  | 
  10 |     await page.goto('/mpa');
  11 |     await expect(page.getByRole('heading', { name: 'MPA Records' })).toBeVisible();
  12 | 
  13 |     await page.getByText('Log In').click();
  14 |     await expect(page.getByText('Welcome Back')).toBeVisible();
  15 | 
  16 |     await page.getByLabel('Username').fill('ada');
  17 |     await page.getByLabel('Password').fill('secret');
  18 |     await page.getByRole('button', { name: 'Sign In' }).click();
  19 | 
  20 |     await expect(page.getByText('Signed in successfully')).toBeVisible();
  21 |     await expect(page.getByRole('button', { name: 'Users' })).toBeVisible({ timeout: 5000 });
  22 | 
  23 |     await page.getByRole('button', { name: 'Users' }).click();
  24 |     await expect(page).toHaveURL(/\/users$/);
  25 |     await expect(page.getByRole('heading', { name: 'Users' })).toBeVisible();
  26 |   });
  27 | 
  28 |   test('redirects unauthenticated users away from protected users page', async ({ page }) => {
  29 |     await setupApiMocks(page, { authenticated: false });
  30 | 
  31 |     await page.goto('/users');
> 32 |     await expect(page).toHaveURL('/');
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  33 |   });
  34 | });
  35 | 
```